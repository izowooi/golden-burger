# Golden Watermelon Soccer stop floor .65 forward cohort v3k

- Cat Soccer entry `[.91,.999]`, Dog Soccer `[.92,.999]`, baseline exact `$5` signal, target `$10` full-depth FOK and `$5` fallback remain unchanged. Their MLB child remains close-only/$5 and NFL active/$5 with stop `.70`.
- Change one common Soccer parameter: absolute stop floor `.70 → .65`. The effective trigger for each newly confirmed BUY is `max(.65, confirmed entry VWAP-.30)`. TP stays disabled and exact resolution hold remains the other exit. Full sellable holding FOK, independent OPEN proof, spread/depth and `-$300` economic guard stay unchanged.
- Selection evidence is exploratory: White 34 earlier resolved games gave the same Cat/Dog displayed-book P&L at `.70` and `.65`; two new White signals improved because the eventual winning Rayo position was not sold at `.69`, while the Real Madrid `.33` gap still stopped. Silver independently observed all three new games. Only four stop events per arm and four UTC game-start days were available; future tail loss and stop slippage are not statistically bounded. `.60` and no-stop had larger retrospective gains but are not deployed because they weaken catastrophe protection further.
- Deploy only when both Soccer live DBs have no `HOLDING`, `PENDING_BUY`, `PENDING_SELL` or `QUARANTINED` position. The current trader evaluates open positions against current policy; an open trade would otherwise receive a retrospective stop change. Do not clear or migrate either DB. New `config_hash × strategy_source_digest × mode × runtime` marks the forward cohort. Existing confirmed fills and earlier losses remain historical evidence.
- This is a forward risk-policy test, not proof of a profit-maximizing stop. Check first natural 1-minute builds, first actual new BUY/stop FOK, the next-day 07:00/16:00 KST reviews, and subsequent independent game days. Reviews never stop new entry automatically. No other sport or account inherits Soccer `$10`/`.65`.
