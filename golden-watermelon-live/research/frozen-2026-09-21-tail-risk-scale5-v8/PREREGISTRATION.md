# Golden Watermelon NFL tail-risk scale `$5` v8

- Cat NFL `.91-.999` and Dog NFL `.94-.999` retain stop floor `.70`, no TP and exact resolution hold. Only target notional changes `$10 → $5`.
- Soccer remains Cat `.91` / Dog `.92`, target `$5`, stop floor `.65`; MLB remains close-only `$5`.
- In the available live Soccer ledger, 23 negative confirmed outcomes never observed even a `.95` best bid before loss/exit. Adding a higher TP therefore did not address the loss tail. No unverified TP or wall-clock time exit is deployed.
- NFL has only 14 resolved raw games and one confirmed live close per arm, below the parameter and scale gate. `$5` limits the single-event tail while more native NFL evidence accumulates.
- Existing holdings keep BUY-time amount and exit parameters. New entry uses the new config/source cohort only.
