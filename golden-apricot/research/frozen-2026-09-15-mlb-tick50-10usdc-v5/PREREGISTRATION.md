# Golden Apricot MLB Tick50 $10 forward cohort v5

- `polybot-eco / apricot-live-eco-mlb-tick50-hold-v1`: full-holding bid VWAP TP `.98`; `polybot-fruit / apricot-live-fruit-mlb-tick50-tp99-v1`: TP `.99`. Both MLB-only BUY targets are `$10`; entry is the first complete HOME/AWAY common tick plus the first valid `[50,52]` minute tick and the midpoint favorite. The `$5` book remains the baseline signal.
- Exact fresh `$10` full-depth ask and FOK confirmation are required. If `$10` is not fully executable, submit one `$5` FOK BUY or no order; intermediate `$6/$7/$8/$9` and partial BUY are outside this cohort contract. Confirmed size/VWAP/fee, not requested amount, determines position cost and all later exits.
- TP uses the full confirmed holding's bid VWAP and full-holding FOK SELL. If TP is never confirmed, use exact token-aligned resolution. No stop/time exit or other sport is added. Ten-position/event/cycle caps `10/1/5`, experiment capital `$100`, and confirmed economic loss guard `-$300` remain unchanged.
- Earlier `$5`/`$10` cohorts and unresolved entry-time positions retain their original entry parameters. The new `config_hash × strategy_source_digest × mode × runtime` starts only after successful deployment. Existing DBs are not cleared, merged or migrated.
- The prior 180-game displayed-book replay was positive at `$5`, but its frozen 35-game validation was negative for both arms and daily-cluster sensitivity included zero. The `$10` forward result must be judged by confirmed FOK fills, all game days and worst-loss concentration.
- Review after 24 hours and continue until each arm has 100 confirmed closes across at least 10 independent UTC game days, the pre-existing v4 scale gate. These are review gates, not an automatic entry stop. Entry remains live until the operator explicitly changes it.
