# Golden Black is accountless and rejects every credential-like variable.
POLYBOT_LIFECYCLE_MODE=archive_only
POLYBOT_SIMULATION_MODE=true

# These are documentation-only constants; config.yaml is frozen for the cohort.
# POLYBOT_PRIVATE_KEY and POLYMARKET_* credentials must never be supplied.
